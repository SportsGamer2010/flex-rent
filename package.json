{
  "name": "flex-rent",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @flex/api\" \"npm run dev -w @flex/web\"",
    "build": "npm run build -w @flex/web",
    "start": "npm run start -w @flex/api",
    "typecheck": "npm run typecheck -w @flex/api && npm run typecheck -w @flex/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
